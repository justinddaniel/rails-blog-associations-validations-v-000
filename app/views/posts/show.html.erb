<p id="notice"><%= notice %></p>

<p>
  <h3>Name:</h3>
  <%= @post.name %>
</p>

<h2><%= @post.name %></h2><br>
<p><%= @post.content %></p><br>
<ul> <% Tag.all.each do |t| %>
  <% if t.post_id == @post.id %>
  <li> <%= t.name %></li><br>
  <% end %>
</ul>
<% end %>


<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
